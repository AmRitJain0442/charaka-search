# Optional: enables long-tail query interpretation through Vercel AI Gateway.
# Vercel deployments authenticate through their automatically supplied OIDC token.
ENABLE_AI_EXPANSION=false

# Use this instead of OIDC when running outside Vercel.
# AI_GATEWAY_API_KEY=
